#first part
# all this program are in same folder
# you can keey it in any folder but you have to
# provide correct path while importing
import module_one
import module_two

number = float(input("Enter number: "))
print(module_one.approximate_sqrt(number))
print(module_two.approximate_sqrt(number))